package com.lapismc.lapis.launcher.core

/**
 * Reference to an instance that has been installed.
 * @param instance Instance that is installed.
 * @param store Storage where the instance is installed.
 */
class InstalledInstance(val instance: Instance, val store: InstanceStore) {
}